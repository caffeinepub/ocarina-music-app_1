{
  "frontend/src/components/SheetMusic.tsx": "Sheet music notation SVG component with treble clef staff, noteheads, stems, pitch labels, and inline tablature diagrams; now accepts an optional fingeringMap prop to reflect user-customized fingering patterns.",
  "frontend/src/components/SampleUploadPanel.tsx": "Panel listing all 8 diatonic notes with upload/remove controls for custom WAV/MP3 audio samples, showing upload progress and notifying the parent of sample URL changes.",
  "frontend/src/components/ImageRecognitionPanel.tsx": "Image upload and client-side sheet music recognition panel that analyzes uploaded PNG/JPEG images via Canvas API, displays recognized notes with editable pitch/duration selectors, and allows loading the result into the score editor.",
  "frontend/src/App.tsx": "Root application component restoring the original structure with all existing panels (PianoKeyboard, SheetMusic, NoteEditor, PlaybackControls, PresetSongLibrary, SampleUploadPanel, ImageRecognitionPanel, FingeringSettingsPanel) plus the new OcarinaShowcasePanel at the top; passes correct props matching existing component interfaces and handles profile play callbacks to load external images into OcarinaVisual.",
  "frontend/src/components/OcarinaVisual.tsx": "Ocarina visual component rendering either the original SVG illustration or a custom/external image with 4 draggable resizable tone hole overlays; adds an `externalImageUrl` prop that updates the displayed image when a profile is loaded from the showcase panel, with local uploads taking priority.",
  "frontend/src/hooks/useFingeringMap.ts": "Extended custom hook that manages fingering map state, hydrates from the backend on startup via useLoadFingeringMap, exposes saveToBackend (calls useSaveFingeringMap), and tracks isDirty/isSaving/isLoadingFromBackend states.",
  "frontend/src/components/FingeringSettingsPanel.tsx": "Extended fingering settings panel with a new \"Global Controls\" section at the top containing a Save Fingering Map button with loading spinner, an unsaved-changes/saved status indicator, and toast notifications; the existing per-note hole toggle grid and Reset button remain below.",
  "frontend/src/components/TablatureDiagram.tsx": "Small SVG tablature diagram component showing a 2×2 hole grid on an ocarina body, now accepting optional fingeringMap and note props to reflect custom fingering overrides.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; restores `useGetPresetSong` as a mutation (for imperative `mutateAsync` usage) and `useAddPresetSong`, while adding new ocarina profile CRUD mutations (useGetOcarinaProfiles, useCreateOcarinaProfile, useBulkCreateOcarinaProfiles, useUpdateOcarinaProfile, useDeleteOcarinaProfile).",
  "frontend/src/components/CreatorPasswordDialog.tsx": "Modal dialog using shadcn/ui Dialog that prompts for a creator password before allowing profile mutations; encodes the input via btoa before passing to the parent callback, never storing plain text.",
  "frontend/src/components/OcarinaProfileCard.tsx": "Profile card component displaying an ocarina image, name, size/pitch footer label (e.g. \"Alto – C5\"), and a Play Ocarina button; supports optional edit/delete action buttons shown on hover.",
  "frontend/src/components/OcarinaProfileForm.tsx": "Upload/edit form dialog for ocarina profiles with fields for name, description, category, shape, multi-file image upload with previews, and a four-option size/pitch preset selector; integrates CreatorPasswordDialog and shows bulk-save progress.",
  "frontend/src/components/OcarinaShowcasePanel.tsx": "Expandable/collapsible horizontal showcase panel at the top of the main page displaying all ocarina profiles in an auto-scrolling strip with left/right arrow navigation, upload button, and edit/delete controls protected by creator password."
}